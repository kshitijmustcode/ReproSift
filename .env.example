# Proposed configuration contract; no application currently consumes it.
# Copy to .env locally. Never commit real values. Empty required values must
# fail startup when their owning component is enabled. Nothing secret is NEXT_PUBLIC_.
NODE_ENV=development
APP_ENV=development
DASHBOARD_PORT=3000
API_PORT=8000
API_BASE_URL=http://localhost:8000
DEMO_STORE_PORT=3001
DEMO_STORE_BASE_URL=http://localhost:3001
ALLOWED_TARGET_ORIGINS=http://localhost:3001
# Add only the exact internal demo-store origin for Docker networking.
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_EMBEDDING_MODEL=
DATABASE_URL=
REDIS_URL=
JOB_BACKEND=celery
MCP_TRANSPORT=stdio
# Required when a separate browser MCP server uses streamable HTTP.
BROWSER_MCP_URL=
ARTIFACT_BACKEND=local
ARTIFACT_DIRECTORY=./artifacts
ARTIFACT_RETENTION_DAYS=7
MAX_ACTIVE_INVESTIGATIONS=1
MAX_PUBLIC_INVESTIGATIONS_PER_DAY=5
RUN_TIMEOUT_MS=300000
ACTION_TIMEOUT_MS=10000
MAX_TOOL_CALLS_PER_RUN=30
MAX_MODEL_CALLS_PER_RUN=15
MAX_OUTPUT_TOKENS_PER_CALL=2000
MAX_TOTAL_MODEL_TOKENS_PER_RUN=60000
REPLAY_COUNT=3
# Trusted harness only; do not expose to browser clients or models.
DEMO_RESET_SECRET=
# Required only when remote authenticated MCP is enabled.
MCP_AUTH_SECRET=
LOG_LEVEL=info
