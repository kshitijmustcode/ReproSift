# Evaluation harness

Reserved for the Python harness, reports and evaluation-only labels. See ../docs/evaluation-plan.md. Never mount this directory into the agent runtime or index it into RAG. Directory separation alone does not enforce isolation; verify tool scopes and deployment mounts. No executable benchmark or results exist yet.
