# @reprosift/mcp-server

TypeScript MCP server exposing scoped Playwright capabilities. Implement the first status tool in Step 7.

Status: workspace placeholder only. No runtime code, framework dependencies or start/build/test scripts are installed yet. Add implementation in the assigned execution-plan step. Keep one root pnpm workspace and lockfile; preserve the scoped package name when scaffolding.
