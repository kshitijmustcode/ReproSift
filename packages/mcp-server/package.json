{
  "name": "@reprosift/mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ReproSift MCP server with a validated status tool over stdio.",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "start": "node dist/cli.js",
    "smoke": "pnpm build && node dist/smoke.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "24.13.4",
    "typescript": "7.0.2"
  }
}
